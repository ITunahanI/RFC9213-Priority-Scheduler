# RFC 9213 Uyumlu Priority Request Scheduler
Bu proje, Min-Heap veri yapısı kullanılarak geliştirilmiş, modüler bir API Gateway istek zamanlayıcısıdır.

## Kurulum ve Çalıştırma
gcc main.c min_heap.c priority_utils.c -o gateway
./gateway
